div.modal-header
    h3.modal-title Authentication required
div.modal-body
    div.alert.alert-danger(ng-show="error") There was a problem logging in. Please try again.
    form(role="form" ng-submit="" novalidate)
        div.form-group
            label(for="username") Username:
            input#username.form-control(type="text", name="username", ng-model="credentials.username")
        div.form-group
            label(for="password") Password:
            input#password.form-control(type="password", name="password", ng-model="credentials.password")
div.modal-footer
    button.btn.btn-primary(ng-click="login(credentials)") Login
